# Jupiter v1.7 — Google Sign-In tasks

**Spec:** [docs/v1.7-google-sign-in-requirements.md](../docs/v1.7-google-sign-in-requirements.md)  
**Prerequisite:** v1.6 email auth (nullable `password_hash`, `email_verified_at`)

---

## Phase 1 — Schema & config

- [x] Drizzle: `oauth_accounts` table + migration
- [x] `users.password_hash` nullable
- [x] `src/server/auth/google.ts` — authorize URL, token exchange, ID token verify
- [x] Env: `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`, `APP_URL`, `AUTH_GOOGLE_ENABLED`
- [x] Update `env.example` + README Google Console setup

## Phase 2 — API routes

- [x] `GET /api/auth/google` — state + PKCE, redirect
- [x] `GET /api/auth/google/callback` — verify, link/create user, session
- [x] Rate limiting on OAuth routes
- [x] Allowlisted `redirect` param (optional)

## Phase 3 — UI

- [x] “Continue with Google” on login + signup (branding-compliant)
- [x] Handle `?error=` on login page
- [x] Hide button when `AUTH_GOOGLE_ENABLED` false (or no client id)

## Phase 4 — Account linking

- [x] Link by verified email to existing password user
- [x] Re-login via same Google `sub`
- [x] Tests: new user, returning user, email collision link

## Phase 5 — Verify

- [x] Manual test: localhost callback registered in Google Console
- [x] Docker deploy with `.env` Google vars
- [x] Document production redirect URI for Vercel/custom domain
