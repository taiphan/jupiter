# Jupiter v1.5 — Implementation tasks

**Spec:** [docs/v1.5-requirements.md](../docs/v1.5-requirements.md)  
**Theme:** Views & workflow (List, Calendar, transition rules, quick filters)

---

## Phase 0 — Setup

- [ ] Create branch `cursor/v1.5-views-workflow`
- [ ] Read spec; confirm tab order with project shell

---

## Phase 1 — Due date foundation (FR-1)

*Blocks calendar and list due-date column.*

- [ ] Add `dueDate?: string` to `issueSchema` in `src/lib/types.ts`
- [ ] Update `issues-store`: `createIssue`, `updateIssue` accept `dueDate`
- [ ] Add `isOverdue(issue)` helper in `src/lib/utils.ts` or `src/lib/derive/due-date.ts`
- [ ] Issue dialog: due date field (date input) + activity log on change
- [ ] Seed: add `dueDate` to ~40% of demo issues
- [ ] JQL-lite: support `dueDate` comparisons in `src/lib/jql.ts`
- [ ] Unit tests: overdue helper, JQL dueDate clauses

---

## Phase 2 — Workflow transition rules (FR-4)

*Can parallel with Phase 1 after types exist.*

- [ ] Define `TransitionRules` type on `Project` in `src/lib/types.ts`
- [ ] Add `DEFAULT_TRANSITION_RULES` in `src/lib/workflow.ts`
- [ ] Implement `canTransition(role, from, to, project): boolean`
- [ ] Unit tests: `src/lib/__tests__/workflow-transitions.test.ts`
- [ ] Board config UI: transition matrix editor (`board-config-view.tsx` or new workflow section)
- [ ] Enforce in `kanban-board.tsx` on drag end — revert + toast if blocked
- [ ] Enforce in issue dialog status `Select`
- [ ] Enforce in backlog status changes (if any inline transitions)

---

## Phase 3 — List view (FR-2)

- [ ] Add `List` tab to `TABS` in `project-shell.tsx`
- [ ] Create `src/app/projects/[key]/list/page.tsx` → `list-view.tsx`
- [ ] Build `IssueListTable` component: sortable headers, row click → dialog
- [ ] Wire `IssueFiltersBar` + sprint filter (reuse board pattern)
- [ ] Column visibility toggle (dropdown: show/hide columns)
- [ ] Persist column prefs to `localStorage`
- [ ] Group-by epic toggle (indent subtasks/stories under epic)
- [ ] Overdue badge on due date column
- [ ] Empty / loading states

---

## Phase 4 — Calendar view (FR-3)

- [ ] Add `Calendar` tab to project shell
- [ ] Create `src/app/projects/[key]/calendar/page.tsx` → `calendar-view.tsx`
- [ ] Month grid component; place issues by `dueDate`
- [ ] Issue chips colored by type; click → issue dialog
- [ ] Drag chip to new day → `updateIssue({ dueDate })`
- [ ] Click empty day → create issue dialog with due date prefilled
- [ ] Footer link: issues missing due date (filter list view)

---

## Phase 5 — Quick filters (FR-5)

- [ ] Define `QuickFilter` type + store slice (extend `projects-store` or new `quick-filters-store`)
- [ ] Built-in chips: All, Assigned to me, Unassigned, Bugs, In progress
- [ ] `QuickFilterBar` component for board + backlog views
- [ ] “Save filter” dialog (name + capture current `IssueFilters`)
- [ ] Delete custom filter (lead/admin)
- [ ] Persist per project in localStorage or Zustand persist

---

## Phase 6 — Polish & docs

- [ ] Update `README.md` feature list for v1.5
- [ ] Update `REQUIREMENTS.md` — mark v1.5 items ✅
- [ ] Accessibility: list table headers, calendar grid labels
- [ ] Manual QA checklist (see spec acceptance criteria)

---

## Phase 7 — Verify & ship

- [ ] `npm test`
- [ ] `npm run build`
- [ ] Fix any linter issues in touched files
- [ ] Commit: `feat(v1.5): list view, calendar, workflow rules, quick filters`

---

## Task dependency graph

```
Phase 1 (due date) ──┬──► Phase 3 (list)
                     └──► Phase 4 (calendar)

Phase 2 (workflow) ──► Phase 3/4 (enforce on inline edits)

Phase 5 (quick filters) — independent; wire last into board/backlog
```

---

## Estimated effort

| Phase | Size |
|-------|------|
| 1 Due date | S |
| 2 Workflow rules | M |
| 3 List view | L |
| 4 Calendar | M |
| 5 Quick filters | S |
| 6–7 Polish | S |

**Total:** ~1–2 focused iterations
