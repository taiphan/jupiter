# v1.11 + v2.0 — Auth breadth & enterprise

**Spec:** [docs/v2.0-auth-enterprise-breadth-requirements.md](../docs/v2.0-auth-enterprise-breadth-requirements.md)

## Prerequisite

- [ ] Complete [v1.10 auth security](./v1.10-auth-security-todo.md) (SMTP + TOTP)

---

## v1.11 — Breadth

### OAuth providers

- [ ] Generalize `oauth_accounts` + shared link/create flow
- [ ] Microsoft Entra OAuth routes + env + UI button
- [ ] GitHub OAuth routes + env + UI button
- [ ] Apple Sign In (stretch) — JWT client secret helper
- [ ] `GET /api/auth/config` lists enabled providers
- [ ] Settings: multi-provider connected accounts card
- [ ] Disconnect routes per provider

### Account & sessions

- [ ] `POST /api/auth/password/set` for OAuth-only users
- [ ] Session metadata (`user_agent`, `ip`) on create
- [ ] `GET /api/auth/sessions`, `DELETE .../:id`, `POST .../revoke-all`
- [ ] Settings → Active sessions UI

### API tokens

- [ ] Schema `api_tokens`
- [ ] CRUD `/api/auth/tokens`
- [ ] Bearer auth middleware + scope checks on workspace/issues APIs
- [ ] Settings → Personal access tokens UI

### Hardening (stretch)

- [ ] Turnstile/hCaptcha on register/login
- [ ] HIBP password check on register/reset
- [ ] `GET /api/auth/security-log`

---

## v2.0 — Enterprise

### Organization model

- [ ] `organizations`, `org_members` tables + migration
- [ ] Default org for existing deployments
- [ ] Admin → Authentication settings page

### SAML

- [ ] `org_sso_config` + SAML SP library integration
- [ ] Metadata, login, ACS routes
- [ ] JIT user provisioning from assertion attributes
- [ ] Operator docs (Okta, Azure AD)

### OIDC enterprise

- [ ] Per-org OIDC config + login/callback
- [ ] `enforce_sso` disables password/social for org members

### SCIM

- [ ] SCIM bearer tokens per org
- [ ] Users + Groups endpoints (RFC 7644 subset)
- [ ] Deprovision → revoke sessions/PATs

### Org policy & audit

- [ ] `org_policies`: require_2fa, email domains, invite_only
- [ ] `org_invites` + invite-only register
- [ ] `auth_events` table + admin paginated API
- [ ] Admin 2FA reset with audit event

---

## Docs

- [ ] REQUIREMENTS.md §7 + roadmap updated
- [ ] env.example future vars documented
- [ ] ARCHITECTURE.md auth diagram (optional)
