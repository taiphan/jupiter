# v1.12 — Issue watchers

**Spec:** [docs/v1.12-watchers-requirements.md](../docs/v1.12-watchers-requirements.md)

## Schema & sync

- [x] `watcherIds` on Issue type + Zod schema
- [x] `issues.watcher_ids` jsonb column + mappers
- [x] Workspace snapshot upsert includes watchers

## Store & derive

- [x] `defaultWatchersForIssue`, `shouldReceiveIssueNotification` helpers
- [x] `toggleWatch`, `addWatcher`, `removeWatcher` on issues store
- [x] Auto-watch reporter + assignee on create; auto-add on assignee change
- [x] Persist migration for legacy issues without `watcherIds`

## Notifications

- [x] Client feed includes watchers (`use-notifications`)
- [x] `GET /api/notifications` includes `watcher_ids @> userId`

## UI

- [x] `IssueWatchers` component in issue dialog
- [x] My Work **Watching** section
- [x] Audit filter label for `watcher` activity kind

## Tests & docs

- [x] `derive/watchers` unit tests
- [x] Issues store watcher tests
- [x] Mapper round-trip test
- [x] CHANGELOG + README + REQUIREMENTS
