# Jupiter v1.6 — Authentication tasks (email-based)

**Spec:** [docs/v1.6-auth-requirements.md](../docs/v1.6-auth-requirements.md)

---

## Phase 1 — Schema & API contract

- [ ] Migration: `users.email_verified_at`, optional `password_reset_tokens`
- [ ] Zod schemas: `login`, `register`, `forgot-password`, `reset-password`
- [ ] Change `POST /api/auth/login` to `{ email, password }`
- [ ] Add `POST /api/auth/register`
- [ ] Rate-limit middleware on `/api/auth/*`

## Phase 2 — Email flows

- [ ] Mailer abstraction (`console` + `smtp`)
- [ ] Verify email token generate/validate + `GET /api/auth/verify`
- [ ] `POST /api/auth/resend-verification`
- [ ] `POST /api/auth/forgot-password` + `POST /api/auth/reset-password`
- [ ] Env documented in `env.example`

## Phase 3 — UI

- [ ] Login form: email + password (remove username)
- [ ] Pages: `/signup`, `/forgot-password`, `/reset-password`
- [ ] Unverified-email banner + resend
- [ ] `auth-store` / `auth-api` use email; dev-only demo fallback

## Phase 4 — Seed & Docker

- [ ] Update `src/server/db/seed.ts` to `@acme.dev` emails + `emailVerifiedAt`
- [ ] Update README + REQUIREMENTS demo table
- [ ] Manual test: OrbStack `npm run deploy` + `db:setup` + email login

## Phase 5 — Tests

- [ ] API route tests: login, register duplicate, reset token expiry
- [ ] `verifyPassword` / validation unit tests (existing + extend)

---

**Next release:** [v1.7 Google Sign-In](../docs/v1.7-google-sign-in-requirements.md)
