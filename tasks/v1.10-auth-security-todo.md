# v1.10 — Gmail SMTP mail & TOTP 2FA

**Spec:** [docs/v1.10-auth-security-requirements.md](../docs/v1.10-auth-security-requirements.md)  
**Release:** `1.10.0` — see [CHANGELOG.md](../CHANGELOG.md)

## A — Gmail / SMTP mail

- [x] Add `nodemailer` and implement `sendAuthEmail` SMTP transport
- [x] `EMAIL_PROVIDER=gmail` + `SMTP_*` env vars documented in `env.example`
- [x] HTML + text templates for verify and reset
- [x] Error handling + tests
- [ ] Vercel production env: Gmail App Password (operator)

## B — TOTP 2FA

- [x] Schema: `totp_secret`, `totp_pending_secret`, `totp_enabled_at`, `totp_backup_codes`
- [x] `POST /api/auth/2fa/setup`, `/enable`, `/challenge`, `/disable`, `/backup-codes/regenerate`
- [x] Login flow: `requires2fa` + `/login/2fa` step
- [x] Settings UI: QR scan, backup codes, disable
- [x] Encrypt secret at rest; hash backup codes
- [x] Rate limit TOTP attempts
- [x] Vitest: TOTP verify window, mail templates, crypto

## C — Admin authentication settings

- [x] `workspace_auth_config` table + encrypted secrets
- [x] `GET/PUT/POST /api/admin/auth-settings` (test email)
- [x] Settings → Authentication & email card (admin)
- [x] Demo emails → `taiphantuan@gmail.com` (+aliases)

## Docs

- [x] REQUIREMENTS.md §7 + CHANGELOG + README v1.10
- [ ] Operator runbook: Gmail App Password + 2FA recovery
