# Copy to .env for docker compose overrides (optional).
# Defaults in docker-compose.yml are enough for local OrbStack deploy.

# DATABASE_URL=postgresql://jupiter:jupiter@postgres:5432/jupiter
# NODE_ENV=production
