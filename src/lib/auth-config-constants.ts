/** Default test / operator email for Jupiter auth (SMTP, redirects, admin seed). */
export const DEFAULT_TEST_EMAIL = 'taiphantuan@gmail.com';

/** Workspace auth toggles default to on; set AUTH_*_ENABLED=false in env to opt out. */
export const DEFAULT_AUTH_PROVIDER_ENABLED = true;
export const DEFAULT_TWO_FACTOR_ENABLED = true;
